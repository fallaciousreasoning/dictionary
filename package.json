{
  "name": "dictionary",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://fallaciousreasoning.github.io/dictionary",
  "dependencies": {
    "@types/react": "^16.9.11",
    "@types/react-dom": "^16.9.4",
    "JSONStream": "^1.3.5",
    "fetch-readablestream": "^0.2.0",
    "localforage": "^1.7.3",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0",
    "typescript": "^3.7.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -rf docs/ && mv build/ docs/",
    "deploy": "npm run build && git add docs/ && git commit -m Deploy && git push",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
